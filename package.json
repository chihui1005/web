{
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000 --strictPort",
    "dev:frontend": "vite --host 0.0.0.0 --port 3000 --strictPort",
    "dev:backend": "python3 -m uvicorn backend.app.main:app --reload",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "preview:3000": "vite preview --host 0.0.0.0 --port 3000 --strictPort",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "latest",
    "@testing-library/react": "latest",
    "@testing-library/user-event": "latest",
    "jsdom": "latest",
    "vitest": "latest"
  }
}